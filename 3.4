#include <stdio.h>

int main() {
    float perimeter, side;

    printf("Enter the perimeter of the square: ");
    scanf_s("%f", &perimeter);

    side = perimeter / 4;

    printf("The length of side is: %f", side);

    return 0;
}
